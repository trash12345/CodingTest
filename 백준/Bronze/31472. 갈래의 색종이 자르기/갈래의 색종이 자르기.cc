#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int w;
    cin >> w;

    w *= 2;

    cout << (int)sqrt(w) * 4;

    return 0;
}