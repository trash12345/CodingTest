#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    double a;
    cin >> a;

    cout << fixed;
    cout.precision(15);

    cout << sqrt(a) * 4;

    return 0;
}