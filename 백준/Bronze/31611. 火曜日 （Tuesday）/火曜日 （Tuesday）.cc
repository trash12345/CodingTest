#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int X;
    cin >> X;

    if (X % 7 == 2)
    {
        cout << 1;
    }
    else
    {
        cout << 0;
    }

    return 0;
}