#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int W, H;
    cin >> W >> H;

    cout << fixed;
    cout.precision(1);

    cout << W * H * double(0.5);

    return 0;
}